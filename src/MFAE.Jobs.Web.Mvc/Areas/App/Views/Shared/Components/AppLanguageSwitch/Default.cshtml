@model MFAE.Jobs.Web.Areas.App.Models.Layout.LanguageSwitchViewModel
@if (Model.Languages.Count > 1)
{
    <div class="d-flex align-items-center ms-1 ms-lg-3 dropdown-language" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-start">
        <div class="@Model.CssClass text-white">
            @(Model.CurrentLanguage.DisplayName)
        </div>
    </div>
    <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg-light-primary fw-bold fs-7 w-200px py-4"
     data-kt-menu="true">
        @foreach (var languageInfo in Model.Languages.Where(language => language.Name != Model.CurrentLanguage.Name))
        {
            <div class="menu-item px-3">
                <a href="@Url.Action("ChangeCulture", "AbpLocalization", new {
                             area = "",
                             cultureName = languageInfo.Name,
                             returnUrl = Context.Request.Path + Context.Request.QueryString
                         })" class="menu-link px-3">

                    @languageInfo.DisplayName
                </a>
            </div>
        }
    </div>
}
